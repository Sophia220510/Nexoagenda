"use client";
import { useActionState, useState } from "react";
import { addBusinessUserAsAdmin, type AddBusinessUserState } from "@/app/admin/actions";
function generate(){const chars="ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789!@#$%";const bytes=crypto.getRandomValues(new Uint8Array(18));return Array.from(bytes,(byte)=>chars[byte%chars.length]).join("")}
export function AddBusinessUserForm({businessId}:{businessId:string}){const[state,action,pending]=useActionState(addBusinessUserAsAdmin,{} as AddBusinessUserState);const[password,setPassword]=useState("");if(state.password)return <div className="credential-result"><strong>Usuário criado. Salve agora:</strong><code>{state.username}</code><code>{state.password}</code></div>;return <form action={action} className="form-stack compact"><input type="hidden" name="business_id" value={businessId}/><div className="field-grid"><label>Nome<input name="name" required/></label><label>Username<input name="username" required/></label><label>Senha temporária<div className="input-action"><input name="password" value={password} onChange={(e)=>setPassword(e.target.value)} minLength={10} required/><button type="button" className="button-ghost" onClick={()=>setPassword(generate())}>Gerar</button></div></label><label>Role<select name="role"><option value="PROFESSIONAL">PROFESSIONAL</option><option value="OWNER">OWNER</option></select></label></div>{state.error&&<p className="notice notice-error">{state.error}</p>}<button className="button" disabled={pending}>{pending?"Criando...":"Adicionar usuário/profissional"}</button></form>}
